# pascal-seo プロジェクト

**このリポジトリのルールの正本はこのファイル（`AGENTS.md`）。作業の前に必ず読むこと。**
仕様は `docs/RULES.md`（コードより優先）、判断の理由は `docs/DECISIONS.md`、未決は `docs/TODO.md`。

落としてはいけないのはこの3つ。

- **作業を頼まれたら、まずリポジトリ直下の `書斎からの指示.md` を読む**
- **空のときは勝手に始めない。** 何をするか聞く
- **そこに無いことを、会話の流れで決めて実装しない**

終わったら `書斎からの指示.md` の `## 結果` に書き戻す。書くのは
①やめた案とその理由 ②人に確認しないと進まないこと ③記録と実装の食い違い の3つだけ。
**実装報告は書かない**（`git log` で分かるので二重管理になる）。1行も書かれないのが正常。

秘密の値は1Password（保管庫 `dev`）。**平文の `.env` を作らない。**

**見積もりで実装量を理由に切り分けない。**

このプロジェクトは、SEOツール「パスカル（Pascal）」を使ったSEO対策の**壁打ち（相談相手）**を目的とする。

## 文脈
- ユーザー（Hide）はパスカルを使ってSEOを進めており、Claudeに相談しながら施策を決めたい。
- プロジェクト直下にパスカルのヘルプPDFが15本ある（機能・ワークフローの一次情報）。
- パスカルの機能・指標・勘どころは **`パスカル機能リファレンス.md`** に整理済み。壁打ち時はまずこれを参照する。

## 進め方
- 具体相談が来たら、パスカルの**どの機能をどう使うか**まで落とし込んで提案する。
- 提案時はパスカル特有のルールを守る：1ページ1キーワード、月間検索数500/1000基準、診断の5要素スコア（コンテンツ100点・タグ85点が前提）、再分析は毎時00分をまたぐ、リライト優先 等。
- 一般論で終わらせず、パスカルの画面操作・指標に紐づけて答える。

## 言語
- 基本は日本語で対応。簡潔・直接的に。
